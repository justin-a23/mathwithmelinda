# Session handoff — 2026-07-28

Read this plus `CLAUDE.md`, `TODO-AMPLIFY-GEN-2.md` and `TODO-APPSYNC-AUTH.md`.

## The headline

**Production migrated from Amplify Gen 1 to Gen 2 today.** It is live, verified,
and serving. Gen 1 is still deployed as the rollback and has NOT been deleted.

- Gen 2 prod stack: `amplify-dg6hiwssnna5c-main-branch-6607401b0b`
- Gen 2 prod endpoint: `upxlozpwvnfjnm6uiylrx7lnda.appsync-api.us-east-1.amazonaws.com`
- Gen 1 endpoint (rollback, still live): `irzsqprjcjco5kq7w7g72zm7qy...`
- Sandbox used for rehearsal: `2l2eucnrs5awtdreyxvpbvvw3i...` (`--identifier gen2test`)

Rollback if needed: `git revert 706807f && git push`. Gen 1's API and tables are
untouched and hold the pre-migration data.

## What changed, in order

1. **Year-end cleanup** — 31 orphaned Enrollment rows deleted (33 → 2). Hannah
   and Meredith archived.
2. **Security fixes** (`abc6d52`) — four IDOR vulnerabilities, three-way role
   guards, AppSync key out of a public repo.
3. **Lesson 7 drafted** — Chapter 1 of the Algebra 1 rewrite is complete.
   Content repo, pushed.
4. **Gen 2 migration** — schema ported, backend deployed, 995 records migrated,
   all 8 server routes converted to per-request auth.
5. **Hotfix** (`1926606`) — SSR runtime env vars. See "mistakes" below.
6. **Mail relay closed** (`9d9291c`) — `/api/send-email` was an open relay.
7. **Student identity converted to the sub** (`a5de6be`, `40e60e4`, build #290,
   live) — the migration normalized the data but not the code, which had left
   every student-facing view returning nothing. See below.

## Mistakes I made — know these

**I broke production this morning.** Removing the hardcoded `APPSYNC_API_KEY`
fallback was right, but Amplify Console env vars are NOT available to the
Next.js SSR runtime — only to the build. Every server route touching AppSync
500'd until `amplify.yml` was changed to write them into `.env.production`.
That is what made Justin's year-end run delete two students' Cognito logins and
fail to archive their profiles. Fixed; profiles archived; ordering reversed so
a failure can no longer do that.

**Lesson 7 had two math errors** that survived my own review — a wrong answer
key (Q10) and a word problem yielding half a person (Q13). Both found by
recomputing, not re-reading. Recompute answer keys; do not trust them.

**My first Gen 2 schema draft used `allow.group('student')`** — and that group
has zero members, so every student would have been denied everything. Verify
auth rules against the live Cognito pool, not against what the code implies.

**I called the migration verified when it was only half-verified.** The probes
tested whether a token was AUTHORIZED to read a model, and every one passed.
None tested whether the app's actual queries MATCHED ANY ROWS. They did not:
the migration normalized `studentId` to the Cognito sub and left every client
path filtering on the email, so grades, submissions, messages and video-resume
returned empty from cutover onward. Fixed in `a5de6be`.

Two things hid it. There was no student account left to notice with — the
year-end run had deleted both — and "authorized" reads as "working" when you
are looking at a 13/13 pass line. When verifying a migration, assert on row
counts the app would actually retrieve, not on permission alone.

## Open items, highest value first

1. **Test the identity conversion with a real student account.** Blocks items 2
   and 3 both. The pool has exactly one user (Melinda), so create a throwaway
   student, approve it at `/teacher/students`, sign in as it and check
   `/dashboard`, `/student/grades` and `/student/submissions` show data, then
   submit work on a lesson to exercise the write path. An empty page means the
   filter still does not match; a console GraphQL error means something else.
   Only a human can do this — it needs an account and a password.
2. **Melinda should actually use the app.** Nobody has walked her real screens
   on Gen 2. Her teacher paths were untouched by the identity conversion, so
   this is independent of item 1 and can happen in parallel.
3. **Row-level auth.** Both prerequisites are now met: studentId is a Cognito
   sub in the data AND the client resolves it in one place
   (`app/lib/identity.ts`). Today's rules are still group-level — one signed-in
   student can read another's submissions. Apply to `Submission`, `Message`,
   `VideoWatch` and `Enrollment`:

       allow.group('teacher'),
       allow.ownerDefinedIn('studentId').identityClaim('sub'),

   `.identityClaim('sub')` is REQUIRED, not stylistic. The default identity is
   `sub::username`, which matches neither the stored value nor the access token
   the server routes forward — access tokens carry `sub` but no
   `cognito:username` (app/lib/auth.ts pins `tokenUse: 'access'`, while the
   browser client sends an ID token). `sub` is the only claim in both.

   NOT `ReportCardRecord`: its `studentId` holds a StudentProfile row id, so an
   owner rule there would match nobody. It needs a different mechanism.

   Do this only after item 1. Close before students arrive Aug 28.
4. **Delete the Gen 1 backend** once items 1 and 2 are done. It is the `dev`
   backend environment in the Amplify console. Irreversible — it holds the only
   pre-migration copy of the data. Do not rush it.
5. **Scope `amplify-dev` back down.** It holds `AdministratorAccess`, granted for
   the CDK bootstrap and no longer needed.
6. **2026/2027 academic year** not set up. Wizard at `/teacher/semesters`.
   First day of school is 2026-08-28; end date undecided. Note the wizard applies
   ONE term name to all selected courses, and its default weights (60/20/20) do
   not match last year's (40/30/30).
7. **Chapters 2–12 of Algebra 1** not drafted. Moves at Melinda's pace.

## Security: covered vs not

**Covered, with evidence:** all 26 API routes audited for auth guards; four IDOR
vulnerabilities fixed; S3 key path traversal closed and adversarially tested (17
encodings); role boundaries; the public-API-key exposure (closed by the
migration); field-level leaks — students could read `correctAnswer` and Zoom
`startUrl`, both confirmed against live data and now teacher-only; open mail
relay in `/api/send-email`; runtime dependency advisories.

**NOT covered — no one has looked:**
- S3 bucket policies and public-access settings on both buckets
- The mobile upload-token system (`/api/mobile-upload`, `/snap`)
- Rate limiting coverage (exists on `submit`; unaudited elsewhere)
- XSS/CSRF review of the React surface
- CloudFront configuration
- Whether `ANTHROPIC_API_KEY`, SES and Zoom routes have ever worked in
  production — they had the same env-var dependency and no fallback, so they
  may never have run. Worth asking Melinda if she has ever successfully sent a
  parent email or created a Zoom meeting.

## Gotchas worth knowing

- **`npm audit` reports ~60 high.** They are Next's own nested `sharp` and
  `postcss`. Not reachable: the app renders submissions with plain `<img>` and
  presigned URLs and uses `next/image` nowhere. Do not "fix" by downgrading Next.
- **`npm ci` will fail.** This repo deliberately does not maintain a synced
  lockfile; both build phases do `rm -rf node_modules package-lock.json && npm
  install`. Build #285 failed because I ignored that.
- **`amplify_outputs.json` is gitignored** and holds the API key. Regenerate
  with `npx ampx generate outputs --branch main --app-id dg6hiwssnna5c`.
- **Node 22 needs explicit `.ts` extensions** in `amplify/backend.ts` imports.
- **`verify-gen2-migration.mjs` and the migration script accept
  `GEN2_TEACHER_TOKEN`** — use a teacher Cognito token rather than temporarily
  granting the API key access.
- The classifier blocks `amplify push`, `cdk bootstrap`, and deployment-config
  changes. Those need a permission rule or the user.

## Test suites

    node --experimental-strip-types scripts/test-ownership.ts   # 54
    node --experimental-strip-types scripts/test-roles.ts       # 23
    node --experimental-strip-types scripts/test-lesson-parser.ts # 9
    node --experimental-strip-types scripts/test-identity.ts    # 7

`test-identity.ts` is mostly a source scan rather than unit tests: it fails if
any call site derives a `studentId` from `signInDetails`, which is the specific
regression that broke every student view after the migration.
