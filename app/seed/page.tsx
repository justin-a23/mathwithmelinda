'use client'

export default function SeedPage() {
  return (
    <div style={{ padding: '48px', fontFamily: 'var(--font-body)' }}>
      <h1 style={{ fontFamily: 'var(--font-display)', marginBottom: '24px' }}>Seed Database</h1>
      <p style={{ color: 'var(--gray-mid)' }}>
        This page has been disabled. Courses are managed from the teacher dashboard.
      </p>
    </div>
  )
}
