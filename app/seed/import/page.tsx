'use client'

export default function ImportLessonsPage() {
  return (
    <div style={{ padding: '48px', fontFamily: 'var(--font-body)' }}>
      <h1 style={{ fontFamily: 'var(--font-display)', marginBottom: '24px' }}>Import Lessons</h1>
      <p style={{ color: 'var(--gray-mid)' }}>
        This page has been disabled. Use the lesson editor in the teacher dashboard instead.
      </p>
    </div>
  )
}
